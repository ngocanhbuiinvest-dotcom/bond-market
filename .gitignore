# === 17/07/2026: user chốt commit CẢ DỮ LIỆU lên GitHub ===
# (trước đây ignore *_raw.csv/json + *.xlsx vì "tái tạo được"; nay theo dõi luôn để repo tự đủ:
#  clone về là có ngay data + dashboard, không phải chạy scraper 30-40 phút)

# === File sinh khi chạy / tạm ===
update_log.txt
*.prev

# === Python cache & thư mục nháp ===
__pycache__/
*.py[cod]
_scratch/

# (17/07/2026: VBMA/ trước đây ignore vì là sub-project riêng — user chốt đưa lên repo luôn)

# === OS / editor ===
.DS_Store
Thumbs.db
.vscode/
.idea/

# GIỮ (được theo dõi): *.py, *.ps1, run_update.bat, dashboard_template.html, dashboard.html,
#   dashboard_data.json, *_raw.csv, *_raw.json, *.xlsx, update_state.json, changes_log.csv,
#   alerts_new_latepay.csv, STATUS.md, README.md
